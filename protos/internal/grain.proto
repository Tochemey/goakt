syntax = "proto3";

package internalpb;

option go_package = "github.com/tochemey/goakt/v3/internal/internalpb;internalpb";

message GrainId {
  string kind = 1;
  string name = 2;
  // value represents the actual GrainId value
  string value = 3;
}

// Grain represents the virtual actor information on the wire.
message Grain {
  GrainId grain_id = 1;
  string host = 4;
  int32 port = 5;
}
