---
title: eGo
description: Event sourcing, durable state, CQRS projections, and saga orchestration built on GoAkt.
sidebarTitle: "eGo"
---

**[eGo](https://github.com/Tochemey/ego)** is a companion framework built on top of GoAkt that adds event sourcing,
durable state, CQRS projections, and saga orchestration. It uses Protocol Buffers for commands, events, and state, and
relies on GoAkt for actor execution, supervision, clustering, and remoting.

<Note>eGo is not bundled with GoAkt; install it as its own module.</Note>

## Installation

```bash Installation
go get github.com/tochemey/ego/v4
```

## Why eGo

GoAkt provides the actor runtime: messaging, supervision, clustering, streams. eGo builds on that runtime to solve the
**persistence and workflow** layer:

- **Actor execution**: GoAkt: Yes. eGo: Uses GoAkt.
- **Clustering and remoting**: GoAkt: Yes. eGo: Uses GoAkt.
- **Event sourcing**: GoAkt: No. eGo: `EventSourcedBehavior`.
- **Durable state**: GoAkt: No. eGo: `DurableStateBehavior`.
- **CQRS projections**: GoAkt: No. eGo: Projection runner.
- **Saga / process manager**: GoAkt: No. eGo: `Saga`.
- **Event publishing**: GoAkt: No. eGo: Kafka, Pulsar, NATS, WebSocket.
- **Snapshot recovery**: GoAkt: No. eGo: `SnapshotStore`.
- **Encryption at rest**: GoAkt: No. eGo: Events and snapshots.

## Core behaviors

eGo offers two persistence models. Choose based on whether you need a full event history or just the latest state.

### Event-sourced behavior

`EventSourcedBehavior` captures state changes as domain events. Commands are processed sequentially: each command
produces zero or more events that are persisted to a journal. State is rebuilt by replaying events on recovery.

- **ID**: Unique behavior identifier.
- **InitialState**: Returns the zero-value state (protobuf message).
- **HandleCommand**: Validates the command against current state and returns resulting events.
- **HandleEvent**: Applies a single event to produce the next state.

Additional capabilities:

- **Snapshots**: Periodic state snapshots to reduce replay time on recovery.
- **Event batching**: Accumulate events across commands and flush in a single store write.
- **Retention policies**: Automatically clean up old events and snapshots after a successful snapshot.
- **Event adapters**: Transform persisted events during replay for schema evolution.
- **Encryption**: At-rest encryption of events and snapshots.

### Durable-state behavior

`DurableStateBehavior` persists only the latest state: no event log, no replay. Each successful command produces a new
state version, and only the latest version is stored.

- **ID**: Unique behavior identifier.
- **InitialState**: Returns the zero-value state (protobuf message).
- **HandleCommand**: Validates the command and returns the new state.

This model is a good fit for configuration-style entities, lower-overhead persistence needs, and use cases where audit
replay is not required.

### Choosing a model

- **Persistence**: Event-sourced: Domain events. Durable-state: Latest state.
- **Recovery**: Event-sourced: Replay (optionally from snapshot). Durable-state: Load latest version.
- **History**: Event-sourced: Full audit trail. Durable-state: None.
- **Complexity**: Event-sourced: Higher. Durable-state: Lower.
- **Best fit**: Event-sourced: Business-critical workflows. Durable-state: Configuration, simple CRUD.

## Persistence stores

eGo uses pluggable store interfaces. In-memory stores ship for testing; production backends are in the
[ego-contrib](https://github.com/Tochemey/ego-contrib) repository.

- **Event journal**: Interface: `persistence.EventsStore`. Backends: Postgres, MongoDB.
- **Snapshots**: Interface: `persistence.SnapshotStore`. Backends: Postgres, MongoDB.
- **Durable state**: Interface: `persistence.StateStore`. Backends: Postgres, MongoDB.
- **Projection offsets**: Interface: `offsetstore.OffsetStore`. Backends: Postgres, MongoDB.

Implement any interface to plug in your own backend.

## Engine

The eGo `Engine` manages entity lifecycle, wires persistence, and optionally enables clustering. Create an engine with
`ego.NewEngine(name, eventStore, options...)`, then call `Start` and `Stop` to manage its lifecycle.

- **WithSnapshotStore**: Enable snapshot-based recovery.
- **WithSnapshotInterval**: Snapshot every `n` events.
- **WithTelemetry**: Enable OpenTelemetry traces and metrics.
- **WithLogger**: Pluggable structured logging.
- **WithEncryption**: At-rest encryption for events and snapshots.
- **WithCluster**: Distribute entities across nodes via GoAkt clustering.

### Entity spawn options

- **WithSnapshotInterval**: Snapshot every `n` events for this entity.
- **WithBatchThreshold**: Flush after `n` accumulated events.
- **WithBatchFlushWindow**: Flush after duration `d`, whichever comes first.
- **WithRetentionPolicy**: Auto-cleanup of old events and snapshots.

### Clustering

eGo delegates clustering to GoAkt. Use `WithCluster` to distribute entities across nodes. Entities are partitioned by
identity and placed on nodes transparently. Commands are routed by the framework.

## Event batching

Under concurrent load, individual store writes become a throughput bottleneck. Event batching accumulates events across
commands and flushes in a single write. While a batch is written, incoming commands are stashed and replayed after the
write completes. A threshold of `0` (default) disables batching.

- **< 100us (in-memory)**: Disabled.
- **100us–1ms**: 5–10.
- **1ms–10ms**: 10–50.
- **> 10ms**: 50–100.

## Projections

Projections consume persisted events and build read models. They track progress via an offset store and support retry,
dead-letter handling, and rebuild.

- **Offset tracking**: Resumes from the last committed offset.
- **Dead letters**: Captures failures after retry exhaustion.
- **Rebuild**: `Engine.RebuildProjection` from a chosen point in time.
- **Lag monitoring**: `Engine.ProjectionLag` per shard with OTel metrics.
- **Event adapters**: Schema evolution during consumption.
- **Decryption**: Automatic decryption before handler processing.

## Event publishing

eGo provides publisher APIs for pushing domain changes to external systems.

- **EventPublisher**: Model: Event-sourced events. Connectors: Kafka, Pulsar, NATS, WebSocket.
- **StatePublisher**: Model: Durable-state updates. Connectors: Kafka, Pulsar, NATS, WebSocket.

## Sagas

Sagas coordinate long-running workflows across multiple entities with compensation for failures. Saga state is persisted
using the same event-sourced foundations. Start a saga with `Engine.Saga()` and inspect it with `Engine.SagaStatus()`.
Define compensation logic for timeouts and failures within your saga behavior.

## Event adapters

Event adapters transform persisted events during replay and projection consumption, enabling schema evolution without
data migration. Register transforms via the `eventadapter` package.

## Encryption

eGo supports at-rest encryption of events and snapshots for GDPR and compliance requirements. Enable with
`WithEncryption(encryptor)` on the engine. Use `Engine.EraseEntity(entityID)` for GDPR-style data erasure.

## Observability

When configured with `WithTelemetry()`, eGo emits OpenTelemetry traces and metrics:

- **Traces**: Command processing, persistence writes.
- **Metrics**: Command counters, latency histograms, persisted events, active entities.
- **Projections**: Processing counters, lag, latest offset, events-behind.

## Testkit

eGo ships a scenario-based testkit for validating behaviors without a running store. Use `testkit.New` for event-sourced
and `testkit.NewDurableState` for durable-state behaviors with a Given/When/Then API.

## See also

- [eGo repository](https://github.com/Tochemey/ego): Source code, examples, and changelog
- [ego-contrib](https://github.com/Tochemey/ego-contrib): Postgres and MongoDB store implementations
- [Extensions and Dependencies](/advanced/extensions-and-dependencies): GoAkt extension mechanism that eGo builds on
- [PubSub](/advanced/pubsub): Application-level topic-based messaging
- [Coordinated Shutdown](/advanced/coordinated-shutdown): Graceful shutdown that eGo entities participate in
